## Installation


* Import the project from git using commande line
   `git clone git@xxxxxxx`

* Navigate to the project folder
      `cd ChatMVC`

* Change mysql connection information (file Config/Config.php) :

* Then run the db script `chat.sql`

* App URL : localhost/webroot/
